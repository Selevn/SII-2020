bind uint func fi(uint x, uint y)
{
bind uint z;
z = (1+x)*2;
stop z;
};
go
{
bind uint x;
x=5;
x=x+1;
x=x+3;
extern uint func RandUint(uint a);
extern uint func Ord(symbol q);
bind uint y;
y = RandUint(4);
bind uint z;
bind symbol sa;
sa = 'a';
bind symbol sb;
bind symbol sc;
y = 5;
z = fi(6663,y);
z = Ord(sa);
sa = sa + '1';
stop x;
};
